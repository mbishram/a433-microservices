apiVersion: v1
kind: Service
metadata:
  name: karsajobs-service
  namespace: karsajobs
  labels:
    app: karsajobs
    tier: backend
spec:
  selector:
    app: karsajobs
    tier: backend
  ports:
    - port: 8080
      targetPort: karsajobs
  type: NodePort
